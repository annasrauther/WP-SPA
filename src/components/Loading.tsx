// Import dependencies
import React from "react";

/**
 * Loading component
 *
 * @returns {JSX.Element}
 * @component
 */
const Loading = (): JSX.Element => {
  return <div>Loading</div>;
};

export default Loading;
